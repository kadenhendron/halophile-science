{% set pageTitle = "" %}
{% set pageDescription = "" %}
{% set metaImage = "" %}

{% extends "_layout.twig" %}

{% set inquire = craft.entries.section('inquire').one() %}

{% block content %}

	<section id="inquire__content">
		<h1>inquire</h1>
		{% set inquire = craft.entries.section('inquire').one() %}

		<p>{{ inquire.inquireText|nl2br }}</p>
		<a href="mailto:{{ inquire.email }}">{{ inquire.email }}</a>

		<h1>info</h1>
		<p>{{ inquire.infoText|nl2br }}</p>
	</section>

    <img id="inquire__dish--1" src="img/inquire-dish-1.png"></div>
    <img id="inquire__dish--2" src="img/inquire-dish-2.png"></div>

{% endblock %}
