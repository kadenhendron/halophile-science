{% set pageTitle = "portfolio | halophile.science" %}
{% set pageDescription = "" %}
{% set metaImage = "" %}

{% extends "_layout.twig" %}

{% block content %}

	<section id="portfolio__content">
		{% set portfolioEntries = craft.entries.section('portfolio').all() %}

		{% for entry in portfolioEntries %}
			<div class="portfolio__item">

                {# <div class="portfolio__image"></div> #}
				{% if entry.photos %}
					<img src="{{ entry.photos.one().getUrl('squareGridThumbnail') }}" alt="{{ entry.portfolioItemTitle }}">
				{% endif %}
				<h2 class="portfolio__item__title">{{ entry.portfolioItemTitle }}</h2>
				<p class="portfolio__item__date">{{ entry.date|date('F Y') }}</p>
                
				<p class="portfolio__item__material">{{ entry.materials }}</p>
			</div>
		{% endfor %}
	</section>


{% endblock %}
